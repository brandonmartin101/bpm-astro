import * as React from 'react'
import Button from '@mui/material/Button'

// React function to show all Mui components in a single place
export const MuiReference = () => {
  return (
    <Button variant='contained' color='primary'>
      Button reference
    </Button>
  )
}
